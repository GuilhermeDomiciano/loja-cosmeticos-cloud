# Loja de Cosméticos - Cloud MVP

Mini projeto de controle de loja de cosméticos usando:

- Java (backend)
- AWS simulada com LocalStack
- Docker Compose para subir tudo

## Como subir apenas o LocalStack por enquanto

```bash
docker compose up -d
